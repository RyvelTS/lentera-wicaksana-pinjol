# Google Gemini API Configuration
GEMINI_API_KEY=AIzaS...

# Server Configuration
PORT=3000
NODE_ENV=production

# CORS Configuration (set to actual frontend URL in production)
# For development: https://localhost (because nginx redirects HTTP to HTTPS)
# For production: https://yourdomain.com
CORS_ORIGIN=https://localhost
